# Cards Design Spec v1

Template placeholder for the card apparatus design contract. Project repositories can replace this with their locked design spec.

The template card layer uses Markdown frontmatter, controlled authority IDs, argument arcs, source snippets, synthesis cards, and generated diagnostics.